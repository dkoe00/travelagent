# travelagent

This is an open-source implementation of a multi-agentic travel planning system. Please note that it is currently a work in progress.


## Setup

We are using `uv` to manage dependencies for this project. After cloning the repo, run `uv sync` in your terminal to install the required dependencies.